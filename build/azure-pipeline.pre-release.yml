# Run on a schedule
trigger: none
pr: none

jobs:
- job:  
  displayName: Step1
  pool: 
    vmImage: 'windows-latest'
  steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        # Write your commands here      
        echo 'Hello world'
